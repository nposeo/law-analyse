"""LLM prompts for Ukrainian law extraction."""

EXTRACTION_PROMPT = """
Ти — юридичний аналітик. Проаналізуй статтю закону України та витягни:
1. Норму (що регулюється)
2. Суб'єкт (на кого поширюється)
3. Пояснення простою мовою (зберігаючи юридичну точність)

Текст статті:
{article_text}

Відповідай у форматі JSON згідно схеми.
"""

CRITIQUE_AND_FINALIZE_PROMPT = """
Ти — критик та редактор юридичних інтерпретацій.

Крок 1: Перевір початкову екстракцію на:
- Галюцинації (інформація, якої немає в тексті)
- Неточності у визначенні суб'єкта
- Спрощення, що спотворює зміст

Крок 2: Створи виправлену фінальну версію.

Оригінальний текст: {article_text}
Початкова екстракція: {initial_extraction}

Якщо впевненість < 0.8, встанови confidence_score відповідно та needs_human_review=true.

Оціни свою впевненість від 0.0 до 1.0, враховуючи:
- Чіткість формулювань у законі
- Наявність всієї необхідної інформації
- Складність юридичних термінів

Відповідай ТІЛЬКИ фінальною JSON версією (без пояснень).
"""
